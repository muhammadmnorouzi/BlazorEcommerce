@page "/"
@page "/search/{searchText}"
@page "/{categoryUrl}"
@using BlazorEcommerce.Client.Pages.Products

<PageTitle>Shop Name</PageTitle>

@if (SearchText == null && CategoryUrl == null)
{
    <FeaturedProducts />
}
else
{
    <ProductList />
}
